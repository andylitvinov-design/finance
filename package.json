{
  "name": "ezohata-incoming-ledger",
  "version": "3.0.29",
  "private": true,
  "description": "Static Vercel app for EzoHata incoming ledger.",
  "scripts": {
    "test": "node --test tests/*.test.*",
    "build": "node scripts/build-check.mjs",
    "release-guard": "bash scripts/release-guard.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
