# /save — Durable Agent Memory

Use this command to save durable project memory.

Runtime:

1. Read `agent-memory/active.md` and `agent-memory/index.md`.
2. Extract the reusable lesson.
3. Classify it.
4. Upsert, not append.
5. Merge duplicates and replace contradictions.
6. Ensure active memory has `Apply when`, `Check`, and `Failure if ignored`.
7. Report what changed.
